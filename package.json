{
  "name": "ex1",
  "version": "1.0.0",
  "scripts": {
    "build-watch:with-project-references": "tsc --build --watch tsconfig.with-project-references.json",
    "build-watch:without-project-references": "tsc --watch --project tsconfig.without-project-references.json",
    "start:from-typscript":"ts-node -r tsconfig-paths/register ./service/src/index.ts"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.4.5"
  }
}
